//! ViewModel layer — pure state machines that coordinate Model services.
//! ViewModels depend on domain types and model services, NOT on GPUI rendering.

pub mod home_vm;
pub mod login_vm;
pub mod root_vm;
